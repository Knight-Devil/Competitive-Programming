#include <bits/stdc++.h>
using namespace std;
int main()
{
    while (1)
    {
        int temp;
        cin >> temp;
        if (temp == 42)
        {
            break;
        }
        else
            cout << temp << "\n";
    }

    return 0;
}